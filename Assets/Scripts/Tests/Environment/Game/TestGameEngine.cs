using Game.Assets.Scripts.Game.Environment.Engine;
using Game.Assets.Scripts.Game.External;
using System;

namespace Game.Tests.Controllers
{
    /*
    public class TestGameEngine : IGameEngine
    {
        public TestLevelsManager Levels { get; }
        public TestDefinitions Settings { get; }
        public TestAssets Assets { get; }

        IDefinitions IGameEngine.Settings => Settings;
        IAssets IGameEngine.Assets => Assets;
        ILevelsManager IGameEngine.Levels => Levels;

        private Action<float> _moveTime;

        public TestGameEngine()
        {
            Levels = new TestLevelsManager();
            Settings = new TestDefinitions();
            Assets = new TestAssets();
        }

        public void SetTimeMover(Action<float> moveTime)
        {
            _moveTime = moveTime;
        }
    }
    */
}
