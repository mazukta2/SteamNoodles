namespace Game.Assets.Scripts.Tests.Mocks.Levels
{
    /*
    public class HouseLevel : LevelMockCreator
    {
        public override void FillCurrentLevel()
        {
            var level = new GameObject().AddComponent<LevelView>();
            var screen = new GameObject().AddComponent<ScreenManagerView>();
            //var screenKeeper = new GameObject().AddComponent<ScreenViewKeeper>();
            level.ScreenManager = screen;
            //screen.Screen = screenKeeper;
        }
    }
    */
}
